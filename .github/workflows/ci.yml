name: CI

on:
  pull_request:

jobs:
  pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: extractions/setup-just@v2

      - name: lint
        run: just lint

      - name: fmt
        run: just fmt

      - name: test
        run: just test
